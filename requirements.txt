# Core dependencies
quart==0.18.4  # Stable version with better compatibility
quart-cors==0.5.0
aiohttp==3.9.1
pandas==2.1.4
selectolax==0.3.17

# Korean NLP - Easy Windows installation!
kiwipiepy==0.17.1

# Optional for better performance (can be removed if installation fails)
# cchardet==2.1.7  # Commented out - has issues with Python 3.12
charset-normalizer==3.3.2  # Alternative to cchardet
aiodns==3.1.1
brotli==1.1.0